[package]
name = "file_explorer"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
features = "0.10.0"
fs = "0.0.5"
iced = { version = "0.12.1", features = ["debug", "tokio", "svg"] }
open = "5.1.3"
tokio = { version = "1.37.0", features = ["fs"] }

